# NTP servers from pool.ntp.org project
# Using multiple pools for better reliability
pool pool.ntp.org iburst maxsources 4
pool time.cloudflare.com iburst maxsources 2

# Record the rate at which the system clock gains/losses time
driftfile /var/lib/chrony/drift

# Enable kernel synchronization of the real-time clock (RTC)
# Chrony will update RTC every 11 minutes when synchronized
# Note: rtcfile cannot be used together with rtcsync
rtcsync

# Allow large time steps in the first 3 updates
# Essential when booting with incorrect time from RTC
makestep 1.0 3

# After losing sync, allow one large step to correct time
# This helps when coming back online after internet outage
makestep 0.1 -1

# Ignore large offset at startup (use RTC time as starting point)
# maxslewrate allows faster convergence
maxslewrate 1000.0

# Allow NTP client access from local network.
allow 0.0.0.0/0

# Minimum number of sources required
minsources 1

# Continue serving time even if not synchronized
# Allows acting as NTP server even when offline
local stratum 10

# Directory for NTS keys and state
ntsdumpdir /var/lib/chrony

# Log directory
logdir /var/log/chrony

# Log selection of time sources
log measurements statistics tracking

# Maximum clock frequency error (compensate for temperature)
maxupdateskew 100.0
